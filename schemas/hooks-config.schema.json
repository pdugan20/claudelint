{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/pdugan20/claudelint/main/schemas/hooks-config.schema.json",
  "title": "Claude Code Hooks Configuration",
  "description": "Schema for hooks.json files that define lifecycle event handlers in Claude Code projects",
  "source": "https://code.claude.com/docs/en/hooks",
  "sourceType": "documentation",
  "type": "object",
  "required": ["hooks"],
  "properties": {
    "description": {
      "type": "string",
      "description": "Optional description of this hooks configuration"
    },
    "hooks": {
      "type": "object",
      "description": "Object with event names as keys, each containing an array of matcher groups",
      "properties": {
        "PreToolUse": { "$ref": "#/$defs/matcherGroupArray" },
        "PostToolUse": { "$ref": "#/$defs/matcherGroupArray" },
        "PostToolUseFailure": { "$ref": "#/$defs/matcherGroupArray" },
        "PostToolBatch": { "$ref": "#/$defs/matcherGroupArray" },
        "PermissionRequest": { "$ref": "#/$defs/matcherGroupArray" },
        "PermissionDenied": { "$ref": "#/$defs/matcherGroupArray" },
        "Notification": { "$ref": "#/$defs/matcherGroupArray" },
        "UserPromptSubmit": { "$ref": "#/$defs/matcherGroupArray" },
        "UserPromptExpansion": { "$ref": "#/$defs/matcherGroupArray" },
        "Stop": { "$ref": "#/$defs/matcherGroupArray" },
        "StopFailure": { "$ref": "#/$defs/matcherGroupArray" },
        "Setup": { "$ref": "#/$defs/matcherGroupArray" },
        "SubagentStart": { "$ref": "#/$defs/matcherGroupArray" },
        "SubagentStop": { "$ref": "#/$defs/matcherGroupArray" },
        "PreCompact": { "$ref": "#/$defs/matcherGroupArray" },
        "PostCompact": { "$ref": "#/$defs/matcherGroupArray" },
        "ConfigChange": { "$ref": "#/$defs/matcherGroupArray" },
        "SessionStart": { "$ref": "#/$defs/matcherGroupArray" },
        "SessionEnd": { "$ref": "#/$defs/matcherGroupArray" },
        "WorktreeCreate": { "$ref": "#/$defs/matcherGroupArray" },
        "WorktreeRemove": { "$ref": "#/$defs/matcherGroupArray" },
        "TeammateIdle": { "$ref": "#/$defs/matcherGroupArray" },
        "TaskCreated": { "$ref": "#/$defs/matcherGroupArray" },
        "TaskCompleted": { "$ref": "#/$defs/matcherGroupArray" },
        "InstructionsLoaded": { "$ref": "#/$defs/matcherGroupArray" },
        "Elicitation": { "$ref": "#/$defs/matcherGroupArray" },
        "ElicitationResult": { "$ref": "#/$defs/matcherGroupArray" },
        "CwdChanged": { "$ref": "#/$defs/matcherGroupArray" },
        "FileChanged": { "$ref": "#/$defs/matcherGroupArray" }
      },
      "additionalProperties": false
    }
  },
  "$defs": {
    "matcherGroupArray": {
      "type": "array",
      "description": "Array of matcher groups for an event",
      "items": { "$ref": "#/$defs/matcherGroup" }
    },
    "matcherGroup": {
      "type": "object",
      "description": "A matcher group with optional pattern and array of hook handlers",
      "required": ["hooks"],
      "properties": {
        "matcher": {
          "type": "string",
          "description": "Optional tool name or pattern to match"
        },
        "hooks": {
          "type": "array",
          "description": "Array of hook handlers to execute",
          "items": { "$ref": "#/$defs/hookHandler" }
        }
      }
    },
    "hookHandler": {
      "type": "object",
      "description": "Individual hook handler (command, http, mcp_tool, prompt, or agent)",
      "required": ["type"],
      "properties": {
        "type": {
          "type": "string",
          "description": "The type of hook handler",
          "enum": ["command", "http", "mcp_tool", "prompt", "agent"]
        },
        "if": {
          "type": "string",
          "description": "Permission rule syntax to filter when this hook runs (e.g. 'Bash(git *)')"
        },
        "timeout": {
          "type": "number",
          "description": "Timeout in seconds",
          "minimum": 0
        },
        "statusMessage": {
          "type": "string",
          "description": "Status message displayed while hook runs"
        },
        "once": {
          "type": "boolean",
          "description": "Whether the hook should only fire once per session"
        },
        "command": {
          "type": "string",
          "description": "Shell command to execute (required if type is 'command')"
        },
        "async": {
          "type": "boolean",
          "description": "Whether the command hook runs asynchronously without blocking"
        },
        "asyncRewake": {
          "type": "boolean",
          "description": "If true, runs in the background and wakes Claude on exit code 2 (command-type hooks)"
        },
        "shell": {
          "type": "string",
          "description": "Shell to use for command-type hooks. Defaults to bash.",
          "enum": ["bash", "powershell"]
        },
        "url": {
          "type": "string",
          "description": "URL to send POST request to (required if type is 'http')"
        },
        "headers": {
          "type": "object",
          "description": "Additional HTTP headers for http-type hooks",
          "additionalProperties": { "type": "string" }
        },
        "allowedEnvVars": {
          "type": "array",
          "description": "Environment variable names allowed in header interpolation for http-type hooks",
          "items": { "type": "string" }
        },
        "server": {
          "type": "string",
          "description": "Name of a configured MCP server (required if type is 'mcp_tool')"
        },
        "tool": {
          "type": "string",
          "description": "Name of the tool to call on the MCP server (required if type is 'mcp_tool')"
        },
        "input": {
          "type": "object",
          "description": "Arguments passed to the MCP tool. Supports ${path} substitution."
        },
        "prompt": {
          "type": "string",
          "description": "Prompt text to inject (required if type is 'prompt' or 'agent')"
        },
        "agent": {
          "type": "string",
          "description": "Agent to invoke (required if type is 'agent')"
        },
        "model": {
          "type": "string",
          "description": "Model to use for prompt/agent-type hooks"
        }
      },
      "allOf": [
        {
          "if": { "properties": { "type": { "const": "command" } } },
          "then": { "required": ["command"] }
        },
        {
          "if": { "properties": { "type": { "const": "http" } } },
          "then": { "required": ["url"] }
        },
        {
          "if": { "properties": { "type": { "const": "mcp_tool" } } },
          "then": { "required": ["server", "tool"] }
        },
        {
          "if": { "properties": { "type": { "const": "prompt" } } },
          "then": { "required": ["prompt"] }
        },
        {
          "if": { "properties": { "type": { "const": "agent" } } },
          "then": { "required": ["agent"] }
        }
      ]
    }
  }
}
