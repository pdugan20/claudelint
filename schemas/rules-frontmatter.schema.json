{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/pdugan20/claudelint/main/schemas/rules-frontmatter.schema.json",
  "title": "Claude Code Rules File Frontmatter",
  "description": "Schema for YAML frontmatter in .claude/rules/*.md files for path-scoped rules. Source: https://code.claude.com/docs/en/memory#path-specific-rules",
  "type": "object",
  "properties": {
    "paths": {
      "type": "array",
      "description": "Glob patterns to scope this rule to specific files. Rules without paths apply globally.",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "minItems": 1
    }
  }
}
